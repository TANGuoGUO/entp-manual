[project]
name = "flet-quill-editor"
version = "0.1.0"
description = "FletQuillEditor control for Flet"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Flet contributors", email = "you@example.com" }
]
dependencies = [
    "flet>=0.86.5",
]

[dependency-groups]
dev = [
    "mkdocs",
    "mkdocs-material",
    "mkdocstrings[python]"
]

[project.urls]
Homepage = "https://mydomain.dev"
Documentation = "https://github.com/MyGithubAccount/flet-quill-editor"
Repository = "https://github.com/MyGithubAccount/flet-quill-editor"
Issues = "https://github.com/MyGithubAccount/flet-quill-editor/issues"

[tool.setuptools.package-data]
"flutter.flet_quill_editor" = ["**/*"]

[tool.setuptools]
license-files = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
