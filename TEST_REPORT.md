# ENTP 自强手册 2.0 测试报告

测试日期：2026-08-13  
测试对象：`G:\project\entp` 当前 Flet 2.0 版本  
结论：**全部可见功能已接入统一异常边界。当前 65/65 个自动化测试通过，7 类原生 Flet 页面/弹窗共审计到的交互回调遗漏为 0。**

## 1. 已修复的问题（按优先级）

### P0-1 主线归档不是原子事务

状态：**已修复并通过故障注入。**

- 用例：`ML-09 / ER-02`
- 位置：`database.py:491-518`
- 模拟：当前主线归档时，用 SQLite trigger 强制第二步“更新 current_mainline_id”失败。
- 实际：第一步 `status='已归档'` 留在未回滚事务中；之后一个无关的成功提交会把这个半完成状态一并写入数据库。
- 风险：可能出现“当前主线已经归档，但设置仍指向它”的不一致状态。
- 建议：多语句业务统一使用显式事务上下文；捕获异常后 `rollback()`，成功后一次 `commit()`。

### P0-2 Markdown 同步不是原子写入

状态：**已修复并通过中断写入故障注入。**

- 用例：`MD-07 / ER-04`
- 位置：`markdown_store.py:66-80`，以及其余直接 `Path.write_text()` 调用。
- 模拟：已有任务文档同步时，在覆盖 32 个字符后抛出磁盘写入异常。
- 实际：原文档被截断，只剩半份内容。
- 风险：磁盘空间不足、同步盘占用、程序中断或权限波动时，用户自由记录可能损坏。
- 建议：在同目录写临时文件，`flush + fsync` 后用 `os.replace()` 原子替换；失败时保留旧文档。

### P1-1 Flet 运行期回调缺少统一异常处理

状态：**已修复。** 已增加 `page.on_error`、用户可见 SnackBar、运行期日志和窗口关闭连接释放。

- 用例：`ER-03`
- 位置：`flet_app.py:232` 的页面配置没有设置 `page.on_error`。
- 实际：启动阶段有最外层日志，但按钮回调、数据库锁、Markdown 同步或外部打开失败，没有统一的日志和用户提示机制。
- 风险：使用 `.pyw` 时可能表现为按钮无反应或窗口行为异常，用户无法知道失败原因。
- 建议：增加全局 `page.on_error`，记录 traceback，并通过 SnackBar 提供“操作未完成、数据是否已保存”的明确反馈。

### P1-2 数据层允许空主线和空任务标题

状态：**已修复。** 创建和更新均拒绝空标题；空白新建主线改为临时编辑页，输入标题后才落库。

- 用例：`ML-02 / TK-02`
- 位置：`database.py:565-569`、`database.py:670-704`。
- 实际：标题只做 `strip()`，空字符串仍满足 SQLite 的 `NOT NULL`，可被写入。
- 风险：虽然当前快速输入 UI 会拦截空内容，但其他入口、迁移脚本或未来接口仍能制造无法辨识的空记录。
- 建议：数据库方法在写入前统一校验，空标题抛出 `ValueError`；UI 负责把错误转为就地提示。

### P2-1 候审看板窄屏未真正重排

状态：**已修复并完成 907 × 684 视觉复核。** 窄屏为响应式两列并纵向滚动，不再横向裁切后续状态。

- 用例：`UI-06`
- 位置：`flet_app.py:1025`。
- 实际：907 × 684 截图中，四个阶段仍固定为 276 px 列并横向滚动；第三列只显示一部分。
- 影响：功能可通过横向滚动访问，但不符合“窄屏布局重排而非缩小/裁切”的既定设计原则。
- 建议：窄屏切换为状态分段控件/标签页，或一列折叠分组；宽屏继续使用四列看板。

### P2-2 数据库锁虽然可诊断，但生产交互仍可能显得卡顿

状态：**已修复。** SQLite 等待上限设为 2 秒，运行期错误会提示本次操作未完成并建议重试。

- 用例：`ER-01`
- 结果：测试把 `busy_timeout` 降至 100 ms 后，锁定写入能在 1.5 秒内以 `OperationalError` 结束，数据库没有损坏。
- 风险：生产连接使用 sqlite3 默认等待时长，且 P1-1 的统一 UI 错误处理尚未实现，用户可能先经历数秒停顿，随后看不到清楚提示。
- 建议：显式设置合理的 `busy_timeout`，对可重试写入显示短暂“数据库正在被占用”提示。

## 2. 测试摘要

| 检查 | 结果 |
|---|---:|
| 预先设计的场景 | 103 条 |
| 自动化测试方法 | 65 个（部分方法组合多个场景） |
| 自动化通过 | 65 |
| 失败/预期失败 | 0 |
| 原有专项回归脚本 | 2/2 通过 |
| 原生 Flet 状态截图 | 11/11 生成并人工复核 |
| 实际无窗口启动器 | 1/1 启动、截图、自动退出 |
| 本轮原生交互边界审计 | 7/7 页面或弹窗，遗漏 0 |
| Python 编译检查 | 通过 |
| 正式数据库是否被测试改写 | 否；`entp_manual.db` 修改时间保持 2026-08-13 01:02:21 |

首轮的 5 个 expected failure 已全部改为普通断言并通过。

## 3. 已验证通过的核心变化

### 主线与任务

- 多条主线可以保存和切换，执行区只维护一个当前主线设置。
- 归档/恢复正常路径通过；归档后任务仍存在。
- 最后一条活动主线和系统收集箱不能归档。
- 一条主线可包含多个任务；同一主线最多一个当前推进任务，不同主线可分别保存焦点。
- 执行日志能更新最小下一步、当天成果证据，并可同时完成任务。

### 今日清单与日期更迭

- 今日快速任务进入收集箱和当天账本。
- 任务改名不会覆盖历史账本的标题和主线快照。
- 过期项只出现在过期区；顺延后旧项为 carried、今天生成 planned 项。

### 完整导出与导入

- 使用 SQLite 在线备份产生一致快照，运行中的程序无需先关闭。
- 一个 `.entp.zip` 同时包含 12 张业务表、全部 Markdown、版本信息、表计数、文件大小和 SHA-256 校验。
- 完整往返可恢复主线、任务、每日账本、完成事件、灵感关系、执行记录、设置和用户自由正文。
- 损坏内容、未来格式版本、缺少清单和不安全路径均在替换当前数据前被拒绝。
- 恢复过程中强制制造 Markdown 安装失败，数据库和 Markdown 都能回到导入前状态。
- 确认导入时先在 `backups/` 生成当前工作空间的安全备份；恢复后数据库重新连接并回到当前主线。
- 原生界面已复核宽屏导入摘要弹窗与 920 × 720 窄屏保管箱入口。
- 重复顺延不会重复创建；批量顺延已有今日项时仍保持 `(日期, 任务)` 唯一。
- 模拟跨日刷新后，旧日期账本保持不变，`is_today` 派生到新日期。
- 非法日期会明确失败；空日期查询安全返回。

### 完成日历

- 修复执行区小日历回调晚绑定：月底循环结束时的占位日 `0` 不再污染已生成的日期格。
- 逐一调用 2026 年 2 月的 28 个日期回调，均能保持各自日期；无记录日期显示空状态。
- 日期选择、月份切换与打开每日日志均加入局部异常隔离；单次错误只记录并提示，不再逃逸到 Flet 会话。

- 同一天来自不同主线的完成记录会合并计数，并可按主线筛选。
- 完成后重新打开，历史完成事实仍保留。
- 同一任务同日完成、重开、再完成：日历任务数仍为 1，事件历史保留两次完成动作。
- 历史账本显示当日快照；实测 2026-08-12 显示完成 2/3。

### 全功能异常边界

- 页面每次更新前递归扫描页面树和弹窗树，自动包装所有 `on_click`、`on_submit`、`on_change`、`on_blur` 等同步/异步回调。
- 动态创建的任务详情、主线编辑、灵感详情和弹窗不需要开发者手工逐个记住异常处理。
- 重复刷新不会重复包装同一回调。
- 已区分输入校验、SQLite 锁、文件权限、备份校验和未知错误的用户提示。
- 回调失败时统一回滚尚未提交的 SQLite 事务，并恢复动作前的页面、日期和选中对象状态。
- 原生故障注入截图确认：提示位于底部，主页面仍完整可用，没有 Flet 错误覆盖层。
- 原生回调审计：当前主线 39、执行弹窗 63、候审区 14、今日清单 15、完成日历 32、主线编辑 6、导入弹窗 12，均为 `unprotected=0`。

### 灵感候审与回流

- 新灵感默认未审视、正文和标签为空、默认不关联主线。
- 四种状态转换会记录审视事件，重复设置相同状态不会重复记录。
- 自由标签可保存；非法状态不会写入。
- 任务关联和灵感关系具有幂等性；自关联和无效关系被忽略。
- 独立灵感和有关联主线的灵感均可转成任务并保留来源关系。
- 执行进度会钳制到 0..100，并按执行结果调整状态。

### Markdown

- 主线、任务、灵感、灵感执行记录和每日账本均产生稳定 ID 文件。
- 对象改名不会改变文档文件名。
- 系统区重复同步幂等；用户自由记录不被系统区覆盖。
- 每日 Markdown 使用历史快照，不受任务之后改名影响。
- 分类索引引用的文件均存在。

## 4. UI 交互复核

| 状态 | 视口/结果 | 结论 |
|---|---|---|
| 当前主线 + 任务详情 | 1463 × 752 | 通过；任务列表、详情、Markdown、当前推进清晰 |
| 当前主线 + 任务详情 | 907 × 684 | 通过；列表可读，详情位于响应式后续区域 |
| 当前主线 + 灵感收纳 | 1463 × 752 | 通过；单行输入、回车收起，和“没动力了”动作区分明确 |
| 空白新主线编辑 | 1463 × 752 | 通过；无弹窗/预设表单，标题和正文直接输入 |
| 灵感详情 | 1463 × 752 | 通过；只有标题、状态、标签、空白正文和 Markdown |
| 候审看板 | 907 × 684 | 功能可用，但阶段采用横向滚动，记 P2 |
| 今日清单 | 1463 × 752 / 907 × 684 | 通过；输入与三组结构清楚，窄屏无关键裁切 |
| 历史账本 | 1463 × 752 | 通过；显示快照和完成 2/3，没有新增任务输入框 |
| 完成日历 | 1463 × 752 / 907 × 684 | 通过；宽屏显示日详情，窄屏月历可读且计数明确 |
| `.pyw` 启动入口 | 1463 × 752 | 通过；能启动、渲染并退出 |

UI 测试没有使用浏览器 DOM 自动化，因为这是原生 Flet 桌面窗口而不是 Web URL。测试使用项目内置的 Flet 截图入口并逐张视觉复核。

## 5. 执行环境

- Windows / PowerShell
- Python：项目 `.venv\Scripts\python.exe`
- UI：Flet 原生桌面窗口
- 数据库：SQLite 临时独立数据库
- 测试日期基准：2026-08-13，历史状态重点模拟 2026-08-12

## 6. 复现命令

```powershell
cd G:\project\entp
.\.venv\Scripts\python.exe -m py_compile flet_app.py database.py markdown_store.py backup_service.py launcher_flet.pyw tests\test_feature_matrix.py tests\test_backup_restore.py tests\test_calendar_ui_safety.py tests\test_ui_exception_boundaries.py tests\prepare_ui_qa.py
.\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v
.\.venv\Scripts\python.exe -m tests.test_daily_ledger
.\.venv\Scripts\python.exe -m tests.test_archive_mainline
```

自动化套件当前没有 `expectedFailure`，所有 65 个测试均要求正常通过。

## 7. 测试证据

- 测试计划：`TEST_PLAN.md`
- 自动化用例：`tests/test_feature_matrix.py`
- 完整备份用例：`tests/test_backup_restore.py`
- 日历交互安全用例：`tests/test_calendar_ui_safety.py`
- 全局交互异常边界用例：`tests/test_ui_exception_boundaries.py`
- UI 数据准备：`tests/prepare_ui_qa.py`
- 截图目录：`designs/test-20260813/`
- 代表截图：
  - `current-task-wide.png`
  - `current-inspiration.png`
  - `vault-new-mainline.png`
  - `ideas-detail-wide.png`
  - `ideas-board-compact.png`
  - `today-wide.png`
  - `today-compact.png`
  - `today-history.png`
  - `calendar-wide.png`
  - `calendar-compact.png`
  - `launcher-startup.png`
  - `fixed-ideas-compact.png`
  - `fixed-runtime-error.png`
  - `fixed-vault-new-mainline.png`
  - `fixed-launcher-startup.png`
  - `outputs/backup-vault-compact-clean.png`
  - `outputs/backup-import-confirm-final.png`
  - `outputs/calendar-empty-safe.png`
  - `outputs/boundary-error.png`

## 8. 剩余风险与未完全自动化部分

- 没有等待真实午夜发生；日期更迭使用替换 `today_iso()` 的方式模拟。
- 没有真实耗尽磁盘或修改系统目录权限；使用可控的中断写入模拟。
- 没有调用系统默认 Markdown 应用，以免产生不可控外部窗口；相关代码缺少异常处理，已列风险。
- 没有完成屏幕阅读器、键盘全路径和高 DPI 多显示器人工无障碍测试。
- 系统原生“保存文件 / 选择文件”窗口需要人工点击；自动化从选择后的备份校验、确认、自动安全备份到重新连接均已覆盖。
- “刷新闪烁”只通过 180 ms AnimatedSwitcher 配置和多次启动截图检查，没有高帧率录屏量化。
- 历史 `logs/flet-startup-error.log` 中留有 2026-08-13 00:54 的旧版 `Container(min_height=...)` 错误；本轮当前代码 12 次窗口启动均成功，该日志不是本轮新失败，但日志策略应加入时间/会话分隔，避免误判。
