# ENTP 自强手册 2.0 测试计划

## 1. 测试目标

本轮测试验证产品是否真的支持“好奇心动力回流系统”，而不只是验证页面能否打开。重点检查：

- 当前执行区只突出一条主线，但允许保管多条主线。
- 一条主线可包含多个任务、一个当前推进任务和完整执行记录。
- 灵感可以快速收纳、独立候审、自由补充标签，并在审视后关联或转化为任务。
- 今日清单是按真实日期保存的每日账本；日期更迭不会改写历史。
- 任务完成事实能进入完成日历；任务重新打开不会抹掉历史完成事实。
- 主线、任务、灵感、执行记录和每日账本均有稳定的 Markdown 文档。
- 非正常情形不会造成静默失败、半写入、重复记录或不可恢复的数据损坏。

## 2. 测试原则与环境

- 数据层测试全部使用系统临时目录中的独立 SQLite 数据库，不读写正式的 `entp_manual.db`。
- Markdown 测试使用临时文档目录，不读写正式的 `markdown/`。
- UI 冒烟测试使用独立 QA 数据库；截图完成后窗口自动退出。
- 每条用例先声明前置条件、操作和预期结果，再执行。
- `P0` 为核心数据安全，`P1` 为核心流程，`P2` 为体验或兼容性。

## 3. 功能点总览

| 模块 | 用户能力 | 核心规则 |
|---|---|---|
| 启动与数据 | 首次启动、再次启动、迁移、日志 | 初始化可重复；外键与唯一约束有效；正式数据不被测试污染 |
| 当前主线 | 查看唯一当前主线、编辑自由正文 | 执行区不并列暴露其他主线 |
| 主线保管箱 | 新建、切换、归档、恢复 | 多主线可保存；最后一条活动主线和系统收集箱不可归档 |
| 主线任务 | 快速创建、完成、重开、聚焦 | 一条主线多任务；每条主线最多一个当前推进任务 |
| 执行记录 | 记录行动、结果、阻碍、下一步 | 行动写入日志；结果进入当天证据；可同时完成任务 |
| 今日清单 | 今日新增、过期、完成、恢复、顺延 | 按日期存快照；顺延幂等；历史日期只读 |
| 完成日历 | 月视图、日详情、返回每日账本 | 基于完成事件，而非任务当前状态；未来日期不可制造历史 |
| 候审灵感 | 快速收纳、展开、状态、标签 | 默认独立于主线；内容初始空白；只预设状态，不强迫分类 |
| 灵感关联 | 关联任务、关联灵感、转任务 | 关系不可自关联、不可重复；转化后保留来源 |
| Markdown | 自动同步、自由编辑、外部打开 | 稳定编号；系统区更新但用户区不被覆盖 |
| 响应式 UI | 宽屏、窄屏、详情页、空白编辑 | 字号可读；布局重排而非压缩；关键输入单击可用 |
| 异常恢复 | DB 锁定、写盘失败、对象缺失、启动失败 | 明确报错；事务原子；不静默、不留下半完成状态 |

## 4. 测试用例

状态定义：`未执行 / 通过 / 失败 / 受限`。执行结果统一记录到 `TEST_REPORT.md`。

### A. 启动、结构与迁移

| ID | 优先级 | 场景与操作 | 预期结果 |
|---|---:|---|---|
| ST-01 | P0 | 在空目录创建 Database | 12 张业务表和必要索引创建成功，存在可用当前主线 |
| ST-02 | P0 | 连续两次打开同一个数据库 | 初始化与迁移幂等，不重复种子、不抛异常 |
| ST-03 | P0 | 检查 `foreign_keys`、唯一当前焦点、每日任务唯一性 | 外键开启；约束能阻止无效或重复数据 |
| ST-04 | P1 | 当前主线设置缺失或指向已归档对象后重读 | 自动选择有效活动主线，不返回归档主线 |
| ST-05 | P1 | 用所有 `--view` 入口启动 QA 窗口 | 页面可渲染并自动退出，进程返回 0 |
| ST-06 | P1 | 无窗口启动发生异常 | `logs/startup.log` 或 `logs/crash.log` 留有可诊断信息 |

### B. 主线与保管箱

| ID | 优先级 | 场景与操作 | 预期结果 |
|---|---:|---|---|
| ML-01 | P1 | 新建仅含标题的主线 | 成功创建，不要求期限、愿景或模板字段 |
| ML-02 | P1 | 新建空标题主线 | 数据层保持可预测；UI 不制造无标题垃圾记录 |
| ML-03 | P1 | 编辑主线标题和自由正文 | 保存后重开仍一致，Markdown 同步更新 |
| ML-04 | P0 | 建立多条主线并切换当前主线 | 只有一条当前主线；其他主线仍在保管箱 |
| ML-05 | P1 | 归档非当前主线再恢复 | 状态正确切换，任务和文档不丢失 |
| ML-06 | P0 | 归档当前主线 | 自动切换到另一条活动主线，设置不悬空 |
| ML-07 | P0 | 归档最后一条活动主线 | 操作被拒绝，原主线保持活动 |
| ML-08 | P0 | 尝试归档系统“收集箱” | 操作被拒绝 |
| ML-09 | P0 | 强制模拟归档中途写设置失败 | 整个操作回滚，不出现“已归档但仍为当前”的半状态 |

### C. 任务、当前推进与执行记录

| ID | 优先级 | 场景与操作 | 预期结果 |
|---|---:|---|---|
| TK-01 | P1 | 在同一主线快速创建多个任务 | 全部归属正确，顺序稳定，单击输入/回车即可创建 |
| TK-02 | P1 | 输入纯空白任务 | 不应创建无标题任务 |
| TK-03 | P1 | 编辑标题、说明、下一步、可选期限 | 字段正确保存；期限保持可选 |
| TK-04 | P0 | 依次将两个任务设为当前推进 | 后者成为焦点，前者自动取消；每条主线最多一个焦点 |
| TK-05 | P1 | 在两条不同主线各设焦点 | 两条主线可各自保存一个焦点，执行区只显示当前主线焦点 |
| TK-06 | P0 | 完成任务 | 任务状态、当天账本和完成事件一致 |
| TK-07 | P0 | 完成后重新打开 | 当前状态恢复待执行，但历史完成事件仍存在 |
| TK-08 | P1 | 写执行行动、结果和下一步 | 新增执行日志；下一步更新；当天账本证据更新 |
| TK-09 | P1 | 写执行记录并勾选完成 | 日志与完成事件一次写成，不重复完成记录 |
| TK-10 | P1 | 操作不存在的 task_id | 返回明确错误，不产生孤儿记录 |

### D. 今日清单与日期更迭

| ID | 优先级 | 场景与操作 | 预期结果 |
|---|---:|---|---|
| DL-01 | P1 | 今日输入框新增任务 | 任务进入收集箱并生成当天 planned 账本项 |
| DL-02 | P0 | 任务加入某日后再改任务标题 | 历史账本保留加入当天的标题与主线快照 |
| DL-03 | P1 | 完成当天账本项 | 项目进入已完成分组并记录真实完成时间 |
| DL-04 | P0 | 重新打开已完成账本项 | 当前项回到 planned，`had_completion` 仍为真 |
| DL-05 | P1 | 昨日未完成项在今天查看 | 出现在“过期”而不是今天或已完成 |
| DL-06 | P0 | 顺延一个过期项到今天 | 旧项标记 carried，今天创建新 planned 项并记录来源 |
| DL-07 | P0 | 对同一过期项重复点击顺延 | 第二次不重复创建，今天同一任务只有一项 |
| DL-08 | P1 | 批量顺延多个过期项，其中一个今天已存在 | 只创建缺失项，计数准确且无重复 |
| DL-09 | P1 | 查看历史日期并尝试新增/修改 | UI 为只读，不改写历史账本 |
| DL-10 | P1 | 跨月、跨年浏览前后日期 | 日期计算和标题正确，不出现负天数或错误月份 |
| DL-11 | P0 | 模拟午夜后刷新 `today` 标志 | 新日成为 today，旧日记录不被重写或丢失 |
| DL-12 | P1 | 查询空日期 | 返回 0 统计和空列表，不崩溃 |
| DL-13 | P1 | 输入非法日期字符串 | 明确失败，不写入格式错误的账本日期 |

### E. 完成日历

| ID | 优先级 | 场景与操作 | 预期结果 |
|---|---:|---|---|
| CA-01 | P0 | 同一天完成来自不同主线的任务 | 月历显示总完成数，日详情列出所有主线快照 |
| CA-02 | P0 | 完成后重新打开任务 | 日历完成数不减少，完成事实仍可追溯 |
| CA-03 | P1 | 同一任务同日完成、重开、再次完成 | 完成任务数按账本项计 1；事件历史可保留多次动作 |
| CA-04 | P1 | 点击有记录日期 | 展示标题、主线和真实完成时间，可进入该日账本 |
| CA-05 | P1 | 点击无记录日期 | 显示空状态，不报错 |
| CA-06 | P1 | 前后切换月份与跨年 | 网格、月份标题和选中日正确 |
| CA-07 | P1 | 今天之后的日期 | 不能通过日历伪造未来完成记录 |

### F. 候审灵感与动力回流

| ID | 优先级 | 场景与操作 | 预期结果 |
|---|---:|---|---|
| ID-01 | P1 | 当前主线页快速输入一句灵感 | 回车后收纳成功、输入折叠；灵感默认未审视且不强制关联主线 |
| ID-02 | P1 | 新建灵感只填标题 | 正文、标签为空，不预填分类问题或思考模板 |
| ID-03 | P1 | 在四种状态间移动 | 状态更新并产生审视事件；重复设同状态不重复记事件 |
| ID-04 | P1 | 设置无效状态 | 操作被拒绝或回退到有效状态，不写入非法枚举 |
| ID-05 | P1 | 保存任意自由标签 | 原样保存，不要求预设类别 |
| ID-06 | P1 | 灵感关联任务两次 | 只保留一条关联 |
| ID-07 | P1 | 两条灵感建立有效关系两次 | 只保留一条关系，类型正确 |
| ID-08 | P1 | 灵感自关联或使用无效关系类型 | 不创建关系 |
| ID-09 | P1 | 从有关联主线的灵感转任务 | 任务进入对应主线并保留来源关联 |
| ID-10 | P1 | 从独立灵感转任务 | 进入可用主线/收集路径，不崩溃 |
| ID-11 | P1 | 添加灵感执行记录，进度超过边界 | 进度钳制到 0..100；有行动后进入正在尝试，100 时归档 |
| ID-12 | P1 | 灵感池完全为空时进入审视 | 页面给出空状态，可返回，不强迫填写 |

### G. Markdown 文档

| ID | 优先级 | 场景与操作 | 预期结果 |
|---|---:|---|---|
| MD-01 | P0 | 同步全部对象 | 主线、任务、灵感、执行记录、每日账本均产生文档 |
| MD-02 | P0 | 对象改名后再次同步 | 仍使用稳定 ID 文件名，不产生按标题重复文件 |
| MD-03 | P0 | 用户在系统标记后写自由正文，再次同步 | 自由正文完整保留，仅系统区更新 |
| MD-04 | P1 | 连续两次同步无数据变化 | 输出内容幂等，不反复追加系统区 |
| MD-05 | P1 | 历史每日账本同步 | 使用标题/主线快照，不被当前对象改名污染 |
| MD-06 | P1 | 检查分类索引 | 索引包含有效链接且不引用不存在文件 |
| MD-07 | P0 | 模拟写盘失败 | 抛出可识别错误；不把旧文件截断成半份内容；UI 告知数据库与文档是否部分成功 |
| MD-08 | P2 | 外部 Markdown 应用不存在或关联错误 | 给出明确提示，主程序继续运行 |

### H. 响应式界面与核心交互

| ID | 优先级 | 场景与操作 | 预期结果 |
|---|---:|---|---|
| UI-01 | P1 | 当前主线宽屏与窄屏 | 宽屏分区清晰；窄屏重排而非截断，正文仍可读 |
| UI-02 | P1 | 任务详情打开/关闭 | 列表与详情关系清晰；任务间保留可点击空白；单击即可输入 |
| UI-03 | P1 | 今日清单宽屏与窄屏 | 添加任务输入突出；过期/今日/完成分组可读、可折叠 |
| UI-04 | P1 | 历史今日账本 | 显示日期和只读状态，不出现误导性“添加任务”入口 |
| UI-05 | P1 | 完成日历宽屏与窄屏 | 月网格和日详情不溢出；选中态明显 |
| UI-06 | P1 | 候审看板与灵感详情 | 卡片可单独展开；详情仅保留状态、标签与空白正文 |
| UI-07 | P1 | 新建主线空白编辑 | 不弹预设表单；标题和正文可直接输入 |
| UI-08 | P1 | 当前主线快速收纳灵感 | 入口容易发现，不与“没有动力/审视灵感”动作混淆 |
| UI-09 | P1 | 保管箱归档/恢复 | 状态、提示和可逆性清晰；当前执行区不并列其他主线 |
| UI-10 | P2 | 连续刷新/切换视图 | 无明显白屏闪烁、重复窗口或异常日志 |

### I. 异常、并发与恢复

| ID | 优先级 | 场景与操作 | 预期结果 |
|---|---:|---|---|
| ER-01 | P0 | 第二连接持有 SQLite 写锁，再保存 | 在有限时间内明确失败/提示重试，界面不永久卡死 |
| ER-02 | P0 | 多语句业务中第二条 SQL 人为失败 | 整个业务事务回滚，无半写入泄漏到后续 commit |
| ER-03 | P1 | UI 回调操作不存在对象 | 全局错误处理记录异常并提示用户，窗口不闪退 |
| ER-04 | P0 | Markdown 写入权限/磁盘错误 | 数据与文档状态可辨识，旧文档不被破坏，错误进入日志 |
| ER-05 | P1 | 快速重复点击完成、顺延、关联 | 操作幂等或受唯一约束保护，不重复计数 |
| ER-06 | P1 | 数据库连接关闭后再调用 | 返回可诊断错误，不造成数据文件损坏 |
| ER-07 | P1 | 关闭桌面窗口 | 数据库连接释放，随后可备份/移动数据库文件 |
| ER-08 | P1 | 启动依赖缺失 | 启动器写日志并给出用户可见的排障路径，而不是只闪退 |
| ER-09 | P0 | 日历某个点击回调发生日期或渲染异常 | 局部捕获、写日志并给出简短提示；当前页面和数据库继续可用 |
| ER-10 | P0 | 嵌套页面中的同步点击/输入回调抛出异常 | 自动隔离，提示具体校验错误，不逃逸到 Flet 会话 |
| ER-11 | P0 | 导入导出等异步回调抛出异常 | 异步异常被等待并隔离，窗口继续运行 |
| ER-12 | P1 | 页面多次刷新与重建 | 同一回调只包装一次，不重复提示或重复执行 |
| ER-13 | P0 | 任务详情、弹窗等动态控件加入页面 | 发送到界面前自动获得异常边界，不依赖人工逐个添加 |
| ER-14 | P1 | 输入错误、数据库锁、文件权限、未知异常 | 分别显示可理解的错误信息，不只显示统一“失败” |
| ER-15 | P0 | 回调执行一条 SQL 后异常、尚未提交 | 统一边界回滚未提交事务，半成品不会被后续操作提交 |
| ER-16 | P0 | 回调修改选中任务/页面后异常 | 恢复动作前导航状态，原页面保持可继续使用 |

### J. 完整导出与导入

| ID | 优先级 | 场景与操作 | 预期结果 |
|---|---:|---|---|
| BU-01 | P0 | 导出正在使用的工作空间 | 通过 SQLite 在线备份产生一致快照，不要求关闭程序 |
| BU-02 | P0 | 检查导出包内容 | 包含数据库、全部 Markdown、格式版本、表计数、文件大小和 SHA-256 清单 |
| BU-03 | P0 | 导出后继续修改，再导入原备份 | 主线、任务、历史、关系、设置和用户 Markdown 完整回到导出时状态 |
| BU-04 | P0 | 修改压缩包内数据库但不更新清单 | 导入前被哈希校验拒绝，当前数据完全不动 |
| BU-05 | P1 | 导入未来格式版本或缺少清单的文件 | 明确拒绝并说明原因 |
| BU-06 | P0 | 备份包含 `../`、绝对路径或符号链接 | 拒绝导入，文件不能逃出恢复目录 |
| BU-07 | P0 | 替换数据库成功后，Markdown 安装被强制失败 | 自动恢复导入前的数据库和 Markdown |
| BU-08 | P0 | 导出目标选中当前数据库或受管理 Markdown 目录 | 阻止覆盖实时数据 |
| BU-09 | P0 | 用户确认导入 | 先在 `backups/` 自动导出当前完整工作空间，再开始替换 |
| BU-10 | P1 | 用户取消文件选择或取消确认 | 不创建、不替换任何数据 |
| BU-11 | P1 | 导入完成 | 重新连接数据库、刷新当前主线与各页面，并显示导入数量和安全备份位置 |
| BU-12 | P1 | 导出/导入时数据库被占用或磁盘失败 | 给出明确错误，旧备份和当前工作空间不被截断 |

## 5. 执行顺序

1. 自动化运行数据结构、主线、任务、每日账本、日历、灵感和 Markdown 正常路径。
2. 使用独立数据库执行事务故障、数据库锁和磁盘写入故障注入。
3. 通过 Flet QA 参数渲染宽屏、窄屏和详情状态，保存截图证据。
4. 检查退出码、截图尺寸、日志和临时数据库完整性。
5. 生成 `TEST_REPORT.md`，区分“通过、失败、受环境限制、未覆盖风险”。

## 6. 通过标准

- 所有 P0 正常流程通过。
- P0 数据安全异常若失败，必须明确列为发布阻断项，不得用 UI 冒烟通过替代。
- 核心页面均有宽屏和窄屏证据，且没有不可读、被裁切或关键输入不可达。
- 正式数据库与正式 Markdown 目录未被测试改写。

## 7. 原生桌面 E2E 执行层

本轮增加 `tests/desktop_e2e.py`，直接驱动应用实际创建的 Flet 控件及其 `on_click`、`on_submit`、`on_change`、`on_blur` 回调。每次执行使用独立 SQLite 数据库、独立 Markdown 目录和独立备份目录，不接触正式工作空间。

覆盖矩阵：

| 层级 | 覆盖范围 | 判定方式 |
|---|---|---|
| 原生 UI E2E | 启动、主线创建/切换、任务创建/编辑/执行/完成/重开、今日清单、顺延、日历、灵感收纳/审视、归档/恢复、导出/导入、空输入、异常恢复 | 必须通过真实 Flet 控件回调并核对 SQLite、Markdown 和 UI 状态 |
| 数据契约 | 尚未暴露在当前 Flet UI 的灵感关系/执行/转任务、Markdown 自由正文、可选期限默认值 | 只证明底层模型可用，不计入 UI E2E 通过数 |
| 人工保留 | Windows 原生保存/选择文件窗口 | Flet/Flutter 不暴露 Windows UI Automation 控件树；自动化从选择结果后的校验、确认、恢复继续覆盖 |

原生 E2E 通过标准：失败数为 0；统一回调边界审计的遗漏数为 0；导入失败或交互异常后数据库事务、导航和选中状态可恢复。
