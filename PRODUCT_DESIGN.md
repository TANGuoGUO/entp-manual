# ENTP 自强手册：产品理念与机制设计

## 初始化工作区

全新用户不面对空白页，也不使用阻断式教程弹窗。第一次创建数据库时，系统生成一套可以直接操作的功能导览数据：当前主线解释单一焦点和最小行动；另一条主线解释保管箱；任务解释快速创建、执行记录、Markdown 和完成日历；候审区用四种真实状态解释灵感流转；今日与昨天各有完成事实用于演示日期账本。所有导览内容都是普通数据，用户可以直接改成自己的内容。初始化只发生一次，已有数据绝不覆盖。

## 1. 这不是普通待办软件

产品的第一原则不是“把事情都管理好”，而是让好奇心重新回到现实推进中：

> 主线执行区 ── 新想法快速收纳 ──→ 独立候审区  
> 候审区 ── 审视 / 孵化 / 小实验 ──→ 可选地进入主线或继续独立存在

ENTP 的问题通常不是没有想法，而是思考带来的完成感替代了现实完成。软件因此明确区分：

- **想通了**：只是一个候审思路。
- **做过了**：必须有行动记录。
- **做成了**：必须有日期、结果和成果证据。

产品不压制发散，也不要求每条灵感立刻服务主线。执行时出现的新想法先进入独立候审区；当用户的新鲜感降低、缺少动力时，可以通过审视不同领域的灵感重新唤起好奇心。审视后的灵感可以待孵化、开始小实验、归档，也可以选择关联某条主线，但关联不是必填结果。

## 2. 2.0 只解决一个问题

2.0 名称：**好奇心动力回流系统**。

范围只包含：

1. 保存多条主线，但一次只突出一条当前主线。
2. 把主线任务改写成可验证的最小行动。
3. 记录每天计划了什么、完成了什么、结转了什么。
4. 快速收纳打断执行的新灵感。
5. 没有动力时进入候审区，自由审视灵感，而不是只召回当前主线的相关内容。
6. 用执行结果更新思路，而不是用思路代替执行。

情绪翻译、关系沟通和人生选择虽然有价值，但不进入第一版，以免产品重新变成一个无边界的“什么都能记”应用。

## 3. 核心对象

### 主线

可以保存多条主线，但执行区一次只突出一条当前主线。其他主线不会删除，也不会被系统判定为失败，只是暂时不占据执行注意力。

新建主线不使用弹窗表单。点击后直接打开一张空白主线文档，只有标题和自由正文；不询问“为什么推进”、目标、周期或其他预设问题。用户写下多少内容由用户决定，离开输入框时自动保存。

导航把“当前主线”和“我的主线任务保管箱”分成两个互斥页面。执行时只看当前主线，不在侧栏并列展示其他选择；需要重新选择时，用户主动进入保管箱，在明确的“设为当前主线”动作后返回执行页。这样既保留可能性，又不让可能性持续争夺注意力。

主线归档是可恢复的注意力收纳，不是删除。归档会保留主线下的任务、执行历史和 Markdown；归档当前主线时，系统先安全切换到另一条未归档主线。为了避免执行区失去承载对象，最后一条活动主线不能被归档。恢复后主线回到保管箱，但不会自动抢占“当前主线”。

时间限制和复盘周期都是可选设置，默认关闭。只有用户主动需要一个阶段边界时，才可以设置 7 / 14 / 30 天或自定义日期；界面不显示连续天数，也不制造倒计时压力。到期后的复选问题是：

> 已经知道它的缺点之后，我还愿不愿意再选一次？

### 任务与最小行动

任务属于主线。进入“今天”的不是抽象项目，而应当是一个可以开始、可以停止、可以验证的最小行动。

用户可以填写：

- 最小行动：下一次真实动作是什么？
- 执行结果：实际发生了什么？
- 新的下一步：根据这次结果，接下来做什么？

系统自动记录：所属主线、是否为当前推进、任务状态、完成日期、完成时间和执行历史。实际收获来自已经完成的任务和执行记录，不要求用户额外填写表格。这里真正要区分的是“脑中已经想通”和“现实中确实做过”。任务的完成勾选、完成时间与执行结果已经承担这层含义，只有需要外部验证时才由用户在执行结果里记录文件、数字、反馈或实物。

### 思路

思路是候审区里的独立兴趣节点。它可以完全不属于主线，也可以关联主线、任务、其他思路和执行记录。它的价值不是“被整理得很漂亮”，而是帮助用户看清自己为什么被它吸引，并决定继续孵化、做一次小实验或归档。

### 完成日历与每日账本

“今天”不是任务的永久属性，而是一个具体日期的事实快照。每日账本记录：

- 当天计划过的行动。
- 当天真正完成的行动与完成时间。
- 当天未完成的行动。
- 哪些行动被结转到下一天。
- 当天看到的成果证据与复盘。

任务后来改名、转移主线或删除，都不能改写过去某一天看到的内容，因此账本保存标题、主线、优先级和截止日期快照。

完成日历是每日账本的轻量入口：日历只显示发生过的完成事实。勾选任务后，当天自动出现完成记录；点击某一天再进入当天账本查看完整过程。它不计算打卡连续天数，也不把没有完成的日期标红。

### 事件

“加入某天、完成、重新打开、结转”都写入不可覆盖的事件流。任务表保存当前状态，事件流和每日账本保存历史事实。

## 4. 每天的完整生命周期

### 进入今天

打开软件时以本地日期创建/读取当天账本。快速输入只创建今天的一次最小行动，不代表长期任务已经完成。

### 执行中

用户主要看到当前行动，不需要同时整理候审区。新灵感在当前页展开一行空白输入，回车即保存到候审区并收起；它不询问补充说明、类别或与主线的关系，也不把用户带离执行上下文。

### 我卡住了

“我卡住了”在本产品中不是“这个任务发生了阻碍”，而是“我对当前节奏失去兴趣或动力”。它是进入候审区的入口：

1. 不要求用户先解释问题，也不诊断阻碍原因。
2. 展示独立灵感池，而不是只展示与当前主线相关的内容。
3. 用户逐条审视：它为什么吸引我、还想验证什么、兴趣强度如何。
4. 决定放入待孵化、开始一次小实验或归档。
5. 只有用户主动选择时，才关联某条主线或转成主线任务。

候审区平时不要求清零。审视本身就是重新接触兴趣的过程，不需要产出主线连接才算完成。

### 完成

勾选完成时记录本地时间，并保留当天快照。后续恢复任务会新增“重新打开”事件，而不是抹除曾经完成过的事实。

### 日期更迭

午夜后不自动把所有未完成事项伪装成“今天”。昨天保持“未完成”，今天显示一个待决策的过期区：

- **顺延**：昨天保留“已结转”，今天新增同一任务的计划记录。
- **保留未完成**：只保留昨天事实，不占用今天。
- **放弃**（后续功能）：保留原因，不再推进。

如果程序跨午夜一直开着，会在下一次刷新“今日清单”时检测本地日期变化；若用户正在看旧的“今天”，自动切到新一天。若用户本来就在回看历史，则不强行跳走。

## 5. 日期系统必须避免的错误

| 错误 | 后果 | 处理规则 |
|---|---|---|
| 用 `is_today` 布尔值表示今天 | 第二天仍然显示为今天 | 以 `YYYY-MM-DD` 的每日账本为准 |
| 完成只改当前状态 | 无法回答哪天完成 | 保存 `completed_at` 与完成事件 |
| 顺延直接改截止日期 | 昨天未完成的事实消失 | 旧记录标记结转，新日期新增记录 |
| 历史页连接任务当前标题 | 改名后过去被篡改 | 每日记录保存字段快照 |
| 恢复任务直接删除完成事实 | 周复盘数据失真 | 写入“重新打开”事件 |
| SQLite UTC 时间和本地日期混用 | 午夜附近记录落错天 | 日期与事件统一使用本地时区时间 |
| 程序关闭多日只处理“昨天” | 漏掉中间日期 | 查询全部早于今天且仍未处理的记录 |
| 多次启动重复迁移/结转 | 每日清单出现重复项 | `(日期, 任务)` 唯一约束，操作幂等 |
| 删除任务连带删除历史 | 无法复盘真实完成 | 历史表使用快照并允许任务引用为空 |
| 系统时间回拨 | 未来/重复事件异常 | 事件保留完整带时区时间，不覆盖旧事件 |

## 6. 页面设计

### 今日清单 / 每日账本

保留滴答式清单的低摩擦体验，但增加日期语义：

- 标题左侧保持清单感，右侧提供前一天、后一天、月历入口。
- 今天显示快速输入；历史日期不允许误添加“今天任务”。
- 今天分为“已过期 / 今天 / 已完成”；历史日分为“当日未完成 / 已完成”。
- 月历上的蓝点表示那一天有行动记录。
- 历史顶部显示完成数，并强调这是不会被后续编辑覆盖的账本。
- 完成项显示真实完成时间；历史页只读，防止误改过去。

完成日历使用独立页面：左侧显示整月完成分布，右侧显示所选日期的完成详情；窄窗口改为上下排列。它读取不可覆盖的完成事件，而不是只读取任务当前状态，因此任务完成后重新打开，日历中的完成事实仍然保留。

### 视觉语言

- 不使用生硬的满屏直角框；内容对象采用 11–13 px 圆角卡片。
- 卡片以细灰边框、2–3 px 克制层次和 7–8 px 垂直间距区分，不堆叠厚重阴影。
- 快速输入、历史提示、看板列和侧栏选中态使用更大的 12–16 px 圆角。
- 按钮使用约 10 px 圆角；主操作保持蓝色，次操作使用浅灰表面。
- 留白负责主要层级，边框和状态色只承担辅助作用。
- 只保留一层侧栏导航；图标和文字位于同一个点击区域，避免左右两条竖栏表达同一功能。
- “Markdown”属于存储实现，界面统一称为“笔记”或“全部文档”，减少技术词带来的旧式工具感。

### 响应式桌面结构

程序不通过缩小字体来容纳窄窗口，而是改变布局结构：

- `≥ 1320 px`：侧栏展示完整主线库；执行工作区左侧为焦点任务和主线任务，右侧为完成日历。
- `< 1320 px`：侧栏缩至 286 px；执行卡片、任务列表和完成日历改为上下堆叠，保持相同字号。
- `≥ 1720 px`：列表型阅读页面限制在约 1160 px 的舒适行宽，并自动增加两侧留白，避免文字横跨整个屏幕。
- 最小支持窗口为 `780 × 620`；在 `920 × 720` 的常用窄窗口下，主要操作、标题和滚动区域不能被挤出窗口。
- Windows 字体缩放基线提升至 `1.55`，正文通常为 11–13 pt，辅助信息不低于 9–10 pt；窗口变窄时字号保持不变。

### 执行区

只放当前主线的行动、相关验证思路和“我卡住了”。它不是全部任务列表。

### 候审区

它是独立灵感库，同时提供两个层级：候审看板只总览“未审视 / 待孵化 / 正在尝试”，单条审视页只保留标题、自由标签和一张空白 Markdown 记录页。每张卡片都能独立展开，也能用小图标一键孵化、尝试或归档；不使用需要反复点击的状态下拉框，不用每日清空，也不以“整理完所有灵感”为成就。

“已归档”不再是候审阶段，而是独立的灵感回收站。归档只让内容退出当前注意力范围，不删除正文、标签或 Markdown；用户可以进入归档区查看，并一键恢复为“未审视”。

### 思路整理

整理兴趣就是审视灵感。用户可让它继续留在“未审视”，或一键进入“待孵化 / 正在尝试”，也可归档到回收站。正文保持空白，标签由用户自由添加；系统不预设类别、问题模板或思考步骤。

## 7. SQLite 与 Markdown 的职责

SQLite 用于关系、检索、日期账本和事件：

- `tasks`：任务当前状态。
- `task_execution_logs`：每个任务的行动、结果与下一步。
- `daily_entries`：每天看到的任务快照及完成/结转结果。
- `task_events`：计划、完成、重开、结转事件。
- `thoughts` 与关联表：候审灵感和连接网络。
- `execution_logs`：思路的每一次现实验证。
- `app_settings`：当前主线等本地偏好。

Markdown 用于长期记录和外部编辑：

- 主线 → `markdown/主线/Mxxxx.md`
- 任务 → `markdown/任务/Txxxx.md`
- 思路 → `markdown/思路/Ixxxx.md`
- 单次执行记录 → `markdown/执行记录/Lxxxx.md`
- 每日账本 → `markdown/每日/YYYY-MM-DD.md`

每个文档的程序同步区只更新结构化事实；同步区之外永远属于用户。每日文档保留当天行动快照，并提供“当日意图 / 成果证据 / 当日复盘”自由区。

## 8. 开发优先级

### 已落地的底座

- 多主线、任务看板、执行区、候审区和思路关系。
- 每个小块对应独立 Markdown。
- 日期账本、历史回看、月历、完成时间、跨日结转和事件记录。

### 下一阶段

1. 当前主线的 7/14/30 天锁定与复选。
2. “今日唯一推进”及成果证据输入。
3. “我卡住了”记录与本地相关灵感召回。
4. 从召回灵感直接生成最小实验，并把结果回写思路。
5. 周复盘：基于每日账本，而不是基于任务当前状态。
