# 三分钟行动与任务详情浮层 QA

## 对比依据

- 行动区参考：`C:\Users\25244\AppData\Local\Temp\codex-clipboard-35e64f7b-1cf9-4e0d-a1f3-bfec625856e3.png`
- 任务浮层参考：`C:\Users\25244\AppData\Local\Temp\codex-clipboard-2a12715a-c953-4114-af1f-005766b93e44.png`
- 行动区实现：`G:\project\entp\outputs\three-minute-focus.png`
- 任务浮层实现：`G:\project\entp\outputs\task-modal-visual.png`
- 紧凑窗口实现：`G:\project\entp\outputs\task-modal-compact.png`
- 行动区全屏同屏对照：`G:\project\entp\outputs\three-minute-focus-comparison.png`
- 行动区局部同屏对照：`G:\project\entp\outputs\three-minute-focus-focused-comparison.png`
- 任务浮层全屏同屏对照：`G:\project\entp\outputs\task-modal-comparison.png`
- 任务浮层局部同屏对照：`G:\project\entp\outputs\task-modal-focused-comparison.png`

## 视口与状态

- 标准桌面截图：1463 × 752 像素；Flet 页面约 1462.86 × 752 CSS px；截图比例 1.0。
- 紧凑窗口：920 × 720；Flet 页面约 906.29 × 683.43 CSS px；截图比例 1.0。
- 行动区状态：当前任务存在且未完成，展示三分钟提示、直接完成、没动力了、暂存新灵感。
- 任务浮层状态：点击主线任务后，列表和完成日历留在背景中；浮层展示任务状态、标题、正文、当前任务入口和 Markdown 入口。

## 行动区对照

- 用户不再需要先改写“下一步最小行动”。固定提示改为“我们先干三分钟，能坚持三分钟就是胜利”，入口成本从一次编辑降为直接开始。
- 删除行动卡右侧的编辑按钮，避免暗示仍需填写或改写内容。
- 主按钮改为“完成当前任务”。点击后直接完成任务并写入完成日历，不弹出执行记录窗口。
- 保留“没动力了”和“暂存新灵感”，继续承接动力回流与灵感收纳的核心流程。
- 与参考相比，按钮、提示卡和状态条沿用项目现有的圆角、蓝绿琥珀色语义，没有复制参考中与本产品无关的执行记录结构。

## 任务浮层对照

- 任务详情不再覆盖右侧完成日历。列表、当前任务和日历作为背景保留，用户能看见自己从哪一条任务进入详情。
- 浮层略向右偏移，与被点击列表保持可见的空间关系；遮罩降低背景干扰，但没有完全切断上下文。
- 任务内容保持自由输入：只展示标题和大块正文，不预设“下一步行动”等额外字段。
- 标题和正文离开输入框自动保存；关闭、切换完成状态和设为当前任务前也会保存。
- 920 × 720 下浮层仍完整显示，底部 Markdown 与自动保存提示没有被裁切。

## 必查表面

- 字体与层级：标题、正文、辅助文字层级清楚；没有新增难读的小字号。
- 间距与圆角：浮层为 20 px 圆角，头部、正文、底部三区节奏一致；按钮与现有组件密度一致。
- 颜色：蓝色表示主要动作，绿色表示正在执行，琥珀色表示灵感；完成操作没有使用危险色。
- 图标：继续使用 Flet Material 图标，没有文字符号、占位图或手绘图标。
- 响应式：标准宽屏和 920 × 720 紧凑窗口均无横向溢出、裁切或操作遮挡。
- 交互：详情弹出、自动保存、设为当前、直接完成、完成日历记录和重新打开任务均可用。

## 对比历史

1. 初始问题（P1）：行动区要求用户改写 15 分钟动作，并通过“记录一次执行”打开额外弹窗；任务详情直接替换右侧区域，用户失去列表与日历上下文。
2. 第一轮修正：改为固定三分钟提示，删除编辑入口，完成按钮直接结束任务；详情改成模态浮层。
3. 功能回归（P2）：首轮 E2E 假设测试任务一定不是当前任务，但初始化数据中该任务已是当前任务，导致断言错误。测试改为同时接受“设为当前任务”和“当前任务”两种合法状态。
4. 最终检查：标准与紧凑 E2E 均通过；同屏对照确认浮层位置、背景关系、行动区层级和控件密度符合目标。

## 测试结果

- 单元与功能测试：82 / 82 通过。
- 标准桌面 E2E：16 个 E2E + 2 个数据契约通过，0 失败；报告：`G:\project\entp\outputs\task-modal-e2e-r2-report.json`。
- 920 × 720 响应式 E2E：16 个 E2E + 2 个数据契约通过，0 失败；报告：`G:\project\entp\outputs\task-modal-e2e-compact-report.json`。
- 直接完成断言：点击“完成当前任务”时未创建 AlertDialog，任务完成并进入完成日历。

## 候审状态与桌面窗口图标补充检查

- 修改前：`G:\project\entp\outputs\idea-unreviewed-visual-0814b.png`
- 修改后：`G:\project\entp\outputs\idea-icons-visual-0814c.png`
- 同屏对照：`G:\project\entp\outputs\idea-icons-comparison-0814c.png`
- 视口：两次截图均为 1463 × 752 像素，同一候审看板、同一数据状态。
- “未审视”由空心圆改为带斜线的眼睛，直接表达“还没看”；“待孵化”由容易看成链条的蛋形图标改为嫩芽，表达“先放着生长”。两者与绿色实验烧杯、灰色归档保持清楚区分。
- 四个状态快捷图标在三列卡片中没有挤压、换行或裁切；当前状态继续使用实色圆角底，非当前状态保持透明。
- 桌面标题栏出现 Flet 默认蓝红图标的原因是运行源码时由 Flet 桌面客户端承载窗口，打包图标不会自动覆盖开发窗口。现在运行时显式把 `assets/app-icon.ico` 设置为窗口图标，源码运行和安装版使用同一品牌资源。
- 回归结果：82 / 82 单元与功能测试通过；16 个 E2E + 2 个数据契约通过，0 失败。E2E 已断言窗口图标文件存在，并覆盖“未审视 ⇄ 待孵化”的往返切换。

## 结论

没有剩余 P0、P1 或 P2 问题。候审状态图标的语义和识别度已经改善，窗口与任务栏也改用项目 Logo。

final result: passed

---

# 子任务界面 Design QA

- 版本：2.1.9
- 日期：2026-08-21
- 参考图：`C:\Users\25244\AppData\Local\Temp\codex-clipboard-a21fa00c-8e50-4d54-ab9c-7d7c3a8066a8.png`
- 当前实现：`C:\Users\25244\AppData\Local\Temp\codex-entp-218-final-ui.png`
- 对照图：`C:\Users\25244\AppData\Local\Temp\codex-entp-218-design-comparison.png`

## 结果：PASSED

- 父任务仅保留展开箭头、复选框和标题，子任务仅保留复选框和标题。
- 所有子任务行占满同一个父任务卡片，可用宽度一致，不再随标题长度变化。
- 父子层级、左侧缩进、白色圆角卡片和轻边框与参考图一致。
- 未增加常驻拖动把手、说明文字或尾部操作按钮。
- 真实鼠标拖放已验证：子任务拖到分组标题可提升为父任务，父任务拖到另一个父任务行可缩进为子任务。
- 拖放期间没有新增未捕获异常；数据只在有效落点接受后通过单次 SQLite 事务变更。
